# ── SEGREDOS: nunca sobem ──────────────────────────────────────────────
# A chave do OpenRouter e a senha do admin vivem SÓ no .env local.
*.env
.env
.env.*
!.env.example
!**/.env.example

# ── artefatos de build / dependências ─────────────────────────────────
node_modules/
dist/
*.log
.DS_Store
Thumbs.db

# ── locais de sessão/ferramentas ───────────────────────────────────────
.claude/

# Áudio bruto (grande, fica fora do repo até ser processado/wired)
MUSICA E EFEITO SONORO/
